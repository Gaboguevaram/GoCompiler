package main

import "fmt"

// A minimal example: keywords, identifiers, a string literal and operators.
func main() {
        var greeting string = "hello, world"
        for i := 0; i < 3; i++ {
                fmt.Println(greeting)
        }
}
